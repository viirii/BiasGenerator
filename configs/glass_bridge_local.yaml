run_name: glass_bridge_local_eval
seed: 7
artifacts_dir: artifacts

env:
  kind: glass_bridge

evaluation:
  scenarios:
    - never_share
    - always_share
    - mixed
  episodes_per_scenario: 10

rollout:
  max_turns: 512

checkpoint:
  dir: artifacts/checkpoints
  name: glass_bridge_placeholder.pt

logging:
  dir: artifacts/logs
